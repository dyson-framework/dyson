---

# This is a suite file.  In here, we include a series of tests.

- name: run all tests
  tests:
    - checkboxes
    - frames
    - alerts
    - waits
    - storing
    - navigation
    - cookies
