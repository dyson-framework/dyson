---

first:
  url: "{{ base_url }}/dynamic_loading/1"
  btn_start: css=div#start > button
  hello: css=div#finish > h4

second:
  url: "{{ base_url }}/dynamic_loading/2"
  btn_start: css=div#start > button
  hello: css=div#finish > h4
